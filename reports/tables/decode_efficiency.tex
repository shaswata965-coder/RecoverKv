% Decode throughput and peak memory: full KV (eager, flash), MiKV, ResQ, ours.
% Needs \usepackage{booktabs}. table* spans both columns of a two-column
% template and is the same as table in a one-column one.
%
% Transcribed from two perf-table printouts, both A100-80GB, one GPU, fp16:
%   ours     cache_budget 0.20, q 0.70 (bytes), gate 0.25, local 128, sink 5,
%            window 8
%   full KV  outputs/efficiency_fullkv_2f06bc4 (runs=1, warmup=0, stat=median)
% Throughput is the printed decode tok/s, i.e. B / steady-state TPOT.
% Memory is the whole-run peak allocated, weights included. The printers say
% "GB" but divide MiB by 1024, so it is GiB, 14.96 of it weights.
% OOM at 16K, B = 32 is inferred, not printed: both full-KV arms printed "-"
% there, and their measured peak grows 2.06 (flash) / 2.29 (eager) GiB per
% sequence from B = 8 to 16 at that shape, i.e. ~81 / ~88 GiB at B = 32.
% Bold is best per column among the rows that have numbers; recheck it once
% MiKV and ResQ are filled in.
\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}l cccc cccc@{}}
\toprule
 & \multicolumn{4}{c}{Throughput (tokens/s) $\uparrow$}
 & \multicolumn{4}{c}{Peak memory (GiB) $\downarrow$} \\
\cmidrule(lr){2-5} \cmidrule(l){6-9}
 & \multicolumn{2}{c}{8K decode} & \multicolumn{2}{c}{16K decode}
 & \multicolumn{2}{c}{8K decode} & \multicolumn{2}{c}{16K decode} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7} \cmidrule(l){8-9}
Method & $B{=}1$ & $B{=}32$ & $B{=}1$ & $B{=}32$
       & $B{=}1$ & $B{=}32$ & $B{=}1$ & $B{=}32$ \\
\midrule
Full KV (eager) &          20.5  &          108.0  &          20.7  &           OOM   &          16.15  &          52.19  &          17.28  &           OOM   \\
Full KV (flash) & \textbf{22.2}  &          322.8  & \textbf{22.3}  &           OOM   &          16.04  &          48.51  &          17.05  &           OOM   \\
\midrule
MiKV            &           --   &            --   &           --   &            --   &            --   &            --   &            --   &            --   \\ % TODO
ResQ            &           --   &            --   &           --   &            --   &            --   &            --   &            --   &            --   \\ % TODO
Ours            &          14.8  & \textbf{429.3}  &          14.8  & \textbf{214.3}  & \textbf{15.61}  & \textbf{34.70}  & \textbf{16.24}  & \textbf{54.78}  \\
\bottomrule
\end{tabular}
\caption{Decode throughput and peak GPU memory on one A100-80GB (fp16). Each
run prefills a 256-token prompt, then generates 8K or 16K tokens at batch size
$B$. Throughput is $B$ divided by the steady-state time per output token;
memory includes ${\approx}15$\,GiB of model weights. Full KV keeps the
uncompressed cache, with eager attention or FlashAttention. Ours keeps 20\% of
the full cache by bytes (70\% of it int2) and reads 25\% of the int2 windows
per step. OOM: exceeds the 80\,GiB device. Best per column in bold.}
\label{tab:decode-efficiency}
\end{table*}
